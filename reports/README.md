# reports/

This directory holds **result and diagnostic reports** (e.g. from diagnostic runs, test result summaries). Do not store such reports in `tmp/`; `tmp/` is for ephemeral files only.

See AGENTS.md (Reports) and the CRITICAL section on stdio transport and logging.
