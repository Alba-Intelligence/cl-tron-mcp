{
  "$schema": "https://app.kilo.ai/config.json",
  "mcp": {
    "cl-tron-mcp": {
      "type": "local",
      "command": ["./run-mcp.sh", "--stdio-only"],
      "enabled": true,
      "timeout": 10000
    }
  }
}
